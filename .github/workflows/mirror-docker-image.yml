name: Generic Docker Image Mirror

on:
  workflow_call:
    inputs:
      source_image:
        required: true
        type: string
        description: "Source Docker image to mirror (e.g. docker.io/library/busybox)"
      target_package:
        required: true
        type: string
        description: "Target package name without registry (e.g. owner/image)"
      tag:
        required: true
        type: string
        description: "Version tag of the source image to mirror"

env:
  REGISTRY: ghcr.io

jobs:
  mirror:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - name: Set up ORAS
        uses: oras-project/setup-oras@ccb71166b124a8452d1b72c4476398a909ed4d63 # main with ORAS v1.3.4 support
        with:
          version: 1.3.4

      - name: Log in to registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | oras login "${{ env.REGISTRY }}" --username "${{ github.actor }}" --password-stdin

      - name: Copy image and OCI referrers
        env:
          SOURCE_IMAGE: ${{ inputs.source_image }}
          TARGET_PACKAGE: ${{ inputs.target_package }}
          VERSION: ${{ inputs.tag }}
        run: |
          SOURCE_REF="${SOURCE_IMAGE}:${VERSION}"
          TARGET_REF="${REGISTRY}/${TARGET_PACKAGE}:${VERSION}"
          MAX_RETRIES=5
          BASE_DELAY=15

          # Do not skip existing tags: recursive copy backfills referrers that
          # may have been published after the image itself was mirrored.
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Attempt $i of $MAX_RETRIES..."
            if oras cp --recursive "$SOURCE_REF" "$TARGET_REF"; then
              echo "Successfully copied image and OCI referrers"
              break
            else
              if [ $i -lt $MAX_RETRIES ]; then
                DELAY=$((BASE_DELAY * (2 ** (i - 1))))
                echo "Copy failed (possibly rate limited), retrying in ${DELAY}s..."
                sleep $DELAY
              else
                echo "Failed after $MAX_RETRIES attempts"
                exit 1
              fi
            fi
          done

          SOURCE_DIGEST=$(oras resolve "$SOURCE_REF")
          TARGET_DIGEST=$(oras resolve "$TARGET_REF")
          if [ "$SOURCE_DIGEST" != "$TARGET_DIGEST" ]; then
            echo "Mirrored digest $TARGET_DIGEST does not match source digest $SOURCE_DIGEST"
            exit 1
          fi
