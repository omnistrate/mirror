name: Mirror Docker Images

on:
    workflow_dispatch:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
    

jobs:
  mirror-alpine:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/alpine
      tag: 3.24.1
      target_package: ${{ github.repository_owner }}/alpine
    permissions:
      packages: write
      contents: read

  mirror-busybox:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/busybox
      tag: 1.38.0
      target_package: ${{ github.repository_owner }}/busybox
    permissions:
      packages: write
      contents: read

  mirror-busybox-grafana:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/busybox
      tag: 1.31.1
      target_package: ${{ github.repository_owner }}/busybox
    permissions:
      packages: write
      contents: read
      
  mirror-grafana-image-renderer:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/grafana/grafana-image-renderer
      tag: latest
      target_package: ${{ github.repository_owner }}/grafana-image-renderer
    permissions: 
      packages: write
      contents: read

  mirror-kubectl:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/alpine/kubectl
      tag: 1.36.3
      target_package: ${{ github.repository_owner }}/kubectl
    permissions:
      packages: write
      contents: read

  mirror-local-path-provisioner:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/rancher/local-path-provisioner
      tag: v0.0.31
      target_package: ${{ github.repository_owner }}/local-path-provisioner
    permissions:
      packages: write
      contents: read

  mirror-aws-cli:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/amazon/aws-cli
      tag: 2.36.10
      target_package: ${{ github.repository_owner }}/aws-cli
    permissions:
      packages: write
      contents: read
      
  mirror-grafana:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/grafana/grafana
      tag: 12.3.1
      target_package: ${{ github.repository_owner }}/grafana
    permissions:
      packages: write
      contents: read

  mirror-grafana-11-4-0:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/grafana/grafana
      tag: 11.4.0
      target_package: ${{ github.repository_owner }}/grafana
    permissions:
      packages: write
      contents: read

  mirror-postgres:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/postgres
      tag: 18.3
      target_package: ${{ github.repository_owner }}/postgres
    permissions:
      packages: write
      contents: read

  mirror-nginxdemos-hello:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/nginxdemos/hello
      tag: latest
      target_package: ${{ github.repository_owner }}/nginxdemos-hello
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-25:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: 1.25
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-27:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: 1.27
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-redis:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/bitnamisecure/redis
      tag: latest
      target_package: ${{ github.repository_owner }}/redis
    permissions:
      packages: write
      contents: read

  mirror-bitnami-postgresql:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/bitnami/postgresql
      tag: latest
      target_package: ${{ github.repository_owner }}/bitnami-postgresql
    permissions:
      packages: write
      contents: read

  # --- New mirrors for e2e and integration tests ---

  mirror-aws-load-balancer-controller:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/eks/aws-load-balancer-controller
      tag: v2.17.1
      target_package: ${{ github.repository_owner }}/aws-load-balancer-controller
    permissions:
      packages: write
      contents: read

  mirror-aws-ebs-csi-driver:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/ebs-csi-driver/aws-ebs-csi-driver
      tag: v1.55.0
      target_package: ${{ github.repository_owner }}/aws-ebs-csi-driver
    permissions:
      packages: write
      contents: read

  mirror-csi-attacher:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/csi-components/csi-attacher
      tag: v4.10.0-eksbuild.3
      target_package: ${{ github.repository_owner }}/csi-attacher
    permissions:
      packages: write
      contents: read

  mirror-csi-provisioner:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/csi-components/csi-provisioner
      tag: v6.1.0-eksbuild.2
      target_package: ${{ github.repository_owner }}/csi-provisioner
    permissions:
      packages: write
      contents: read

  mirror-csi-resizer:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/csi-components/csi-resizer
      tag: v2.0.0-eksbuild.3
      target_package: ${{ github.repository_owner }}/csi-resizer
    permissions:
      packages: write
      contents: read

  mirror-csi-node-driver-registrar:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/csi-components/csi-node-driver-registrar
      tag: v2.15.0-eksbuild.3
      target_package: ${{ github.repository_owner }}/csi-node-driver-registrar
    permissions:
      packages: write
      contents: read

  mirror-csi-livenessprobe:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/csi-components/livenessprobe
      tag: v2.17.0-eksbuild.3
      target_package: ${{ github.repository_owner }}/csi-livenessprobe
    permissions:
      packages: write
      contents: read

  mirror-aws-efs-csi-driver:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/efs-csi-driver/amazon/aws-efs-csi-driver
      tag: v2.3.0
      target_package: ${{ github.repository_owner }}/aws-efs-csi-driver
    permissions:
      packages: write
      contents: read

  mirror-eks-distro-csi-livenessprobe:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/eks-distro/kubernetes-csi/livenessprobe
      tag: v2.12.0-eks-1-29-7
      target_package: ${{ github.repository_owner }}/eks-distro-csi-livenessprobe
    permissions:
      packages: write
      contents: read

  mirror-eks-distro-csi-node-driver-registrar:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/eks-distro/kubernetes-csi/node-driver-registrar
      tag: v2.10.0-eks-1-29-7
      target_package: ${{ github.repository_owner }}/eks-distro-csi-node-driver-registrar
    permissions:
      packages: write
      contents: read

  mirror-aws-mountpoint-s3-csi-driver:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: public.ecr.aws/mountpoint-s3-csi-driver/aws-mountpoint-s3-csi-driver
      tag: v2.3.0
      target_package: ${{ github.repository_owner }}/aws-mountpoint-s3-csi-driver
    permissions:
      packages: write
      contents: read

  # Dataplane host-cluster amenities. Release assembly consumes these stable
  # GHCR mirrors instead of pulling third-party registries directly.

  mirror-cluster-autoscaler:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: registry.k8s.io/autoscaling/cluster-autoscaler
      tag: v1.34.2
      target_package: ${{ github.repository_owner }}/cluster-autoscaler
    permissions:
      packages: write
      contents: read

  mirror-external-dns:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: registry.k8s.io/external-dns/external-dns
      tag: v0.20.0
      target_package: ${{ github.repository_owner }}/external-dns
    permissions:
      packages: write
      contents: read

  mirror-cert-manager-controller:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/jetstack/cert-manager-controller
      tag: v1.20.1
      target_package: ${{ github.repository_owner }}/cert-manager-controller
    permissions:
      packages: write
      contents: read

  mirror-cert-manager-webhook:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/jetstack/cert-manager-webhook
      tag: v1.20.1
      target_package: ${{ github.repository_owner }}/cert-manager-webhook
    permissions:
      packages: write
      contents: read

  mirror-cert-manager-cainjector:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/jetstack/cert-manager-cainjector
      tag: v1.20.1
      target_package: ${{ github.repository_owner }}/cert-manager-cainjector
    permissions:
      packages: write
      contents: read

  mirror-cert-manager-startupapicheck:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/jetstack/cert-manager-startupapicheck
      tag: v1.20.1
      target_package: ${{ github.repository_owner }}/cert-manager-startupapicheck
    permissions:
      packages: write
      contents: read

  mirror-nginx-ingress-controller:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/nginx/nginx-ingress
      tag: "3.6.2"
      target_package: ${{ github.repository_owner }}/nginx-ingress
    permissions:
      packages: write
      contents: read

  mirror-prometheus-operator:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/prometheus-operator/prometheus-operator
      tag: v0.90.1
      target_package: ${{ github.repository_owner }}/prometheus-operator
    permissions:
      packages: write
      contents: read

  mirror-prometheus-config-reloader:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/prometheus-operator/prometheus-config-reloader
      tag: v0.90.1
      target_package: ${{ github.repository_owner }}/prometheus-config-reloader
    permissions:
      packages: write
      contents: read

  mirror-prometheus:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/prometheus/prometheus
      tag: v3.11.1
      target_package: ${{ github.repository_owner }}/prometheus
    permissions:
      packages: write
      contents: read

  mirror-cost-prometheus:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: quay.io/prometheus/prometheus
      tag: v2.55.1
      target_package: ${{ github.repository_owner }}/prometheus
    permissions:
      packages: write
      contents: read

  mirror-kube-webhook-certgen:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/jkroepke/kube-webhook-certgen
      tag: "1.8.0"
      target_package: ${{ github.repository_owner }}/kube-webhook-certgen
    permissions:
      packages: write
      contents: read

  mirror-bats:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/bats/bats
      tag: v1.4.1
      target_package: ${{ github.repository_owner }}/bats
    permissions:
      packages: write
      contents: read

  mirror-headlamp:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/headlamp-k8s/headlamp
      tag: v0.41.0
      target_package: ${{ github.repository_owner }}/headlamp
    permissions:
      packages: write
      contents: read

  mirror-dashboard-auth:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/omnistrate-oss/dashboard-auth
      tag: "1.4.0"
      target_package: ${{ github.repository_owner }}/dashboard-auth
    permissions:
      packages: write
      contents: read

  mirror-dashboard-api:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/omnistrate-oss/dashboard-api
      tag: "1.14.0"
      target_package: ${{ github.repository_owner }}/dashboard-api
    permissions:
      packages: write
      contents: read

  mirror-dashboard-web:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/omnistrate-oss/dashboard-web
      tag: "1.7.0"
      target_package: ${{ github.repository_owner }}/dashboard-web
    permissions:
      packages: write
      contents: read

  mirror-dashboard-metrics-scraper:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/omnistrate-oss/dashboard-metrics-scraper
      tag: "1.2.2"
      target_package: ${{ github.repository_owner }}/dashboard-metrics-scraper
    permissions:
      packages: write
      contents: read

  mirror-kong:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/kong
      tag: "3.9"
      target_package: ${{ github.repository_owner }}/kong
    permissions:
      packages: write
      contents: read

  mirror-keda:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/kedacore/keda
      tag: "2.19.0"
      target_package: ${{ github.repository_owner }}/keda
    permissions:
      packages: write
      contents: read

  mirror-keda-metrics-apiserver:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/kedacore/keda-metrics-apiserver
      tag: "2.19.0"
      target_package: ${{ github.repository_owner }}/keda-metrics-apiserver
    permissions:
      packages: write
      contents: read

  mirror-keda-admission-webhooks:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: ghcr.io/kedacore/keda-admission-webhooks
      tag: "2.19.0"
      target_package: ${{ github.repository_owner }}/keda-admission-webhooks
    permissions:
      packages: write
      contents: read

  mirror-dcgm-exporter:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: nvcr.io/nvidia/k8s/dcgm-exporter
      tag: 4.5.2-4.8.1-distroless
      target_package: ${{ github.repository_owner }}/dcgm-exporter
    permissions:
      packages: write
      contents: read

  mirror-bitnamisecure-redis:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/bitnamisecure/redis
      tag: latest
      target_package: ${{ github.repository_owner }}/bitnamisecure-redis
    permissions:
      packages: write
      contents: read

  mirror-bitnamisecure-postgresql:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/bitnamisecure/postgresql
      tag: latest
      target_package: ${{ github.repository_owner }}/bitnamisecure-postgresql
    permissions:
      packages: write
      contents: read

  mirror-postgres-17:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/postgres
      tag: "17"
      target_package: ${{ github.repository_owner }}/postgres
    permissions:
      packages: write
      contents: read

  mirror-redis-stack-server:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/redis/redis-stack-server
      tag: latest
      target_package: ${{ github.repository_owner }}/redis-stack-server
    permissions:
      packages: write
      contents: read

  mirror-pg-with-exporter:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/tomnislav/pg-with-exporter
      tag: "2.0"
      target_package: ${{ github.repository_owner }}/pg-with-exporter
    permissions:
      packages: write
      contents: read

  mirror-noop:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/omnistrate/noop
      tag: latest
      target_package: ${{ github.repository_owner }}/noop
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-25-alpine:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: 1.25-alpine
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-26:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: "1.26"
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-26-alpine:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: 1.26-alpine
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-nginx-1-27-alpine:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/nginx
      tag: 1.27-alpine
      target_package: ${{ github.repository_owner }}/nginx
    permissions:
      packages: write
      contents: read

  mirror-redis-7-2:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/redis
      tag: "7.2"
      target_package: ${{ github.repository_owner }}/official-redis
    permissions:
      packages: write
      contents: read

  mirror-redis-7-4:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/redis
      tag: "7.4"
      target_package: ${{ github.repository_owner }}/official-redis
    permissions:
      packages: write
      contents: read

  mirror-postgres-15-alpine:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/library/postgres
      tag: "15-alpine"
      target_package: ${{ github.repository_owner }}/postgres
    permissions:
      packages: write
      contents: read

  mirror-temporalio-auto-setup:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/temporalio/auto-setup
      tag: "1.29.0"
      target_package: ${{ github.repository_owner }}/temporalio-auto-setup
    permissions:
      packages: write
      contents: read

  mirror-temporalio-ui:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/temporalio/ui
      tag: "2.48.2"
      target_package: ${{ github.repository_owner }}/temporalio-ui
    permissions:
      packages: write
      contents: read

  mirror-testcontainers-ryuk:
    uses: ./.github/workflows/mirror-docker-image.yml
    with:
      source_image: docker.io/testcontainers/ryuk
      tag: "0.13.0"
      target_package: ${{ github.repository_owner }}/testcontainers-ryuk
    permissions:
      packages: write
      contents: read
